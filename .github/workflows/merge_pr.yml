name: Merge dev PR to ${{ env.TARGET_BRANCH }} branch

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  mergePr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Merge into branch ${{ env.TARGET_BRANCH }}
        run: |
          git checkout ${{ env.TARGET_BRANCH }}
          git branch
          git log --oneline
          echo ${{ github.event.pull_request.head.ref }}
          # git merge --no-ff ${{ github.event.pull_request.head.ref }} -m "Merge pull request into ${{ env.TARGET_BRANCH }}"
        env:
          TARGET_BRANCH: alpha
